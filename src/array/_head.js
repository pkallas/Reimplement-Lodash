// Gets the first element of array.

// Create function named _head.
function _head(array) {

// Use shift to obtain and return the first element of the array.
  return array.shift();
}

// Export it.
export default _head; 
