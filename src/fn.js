// Define a function to return the length of a specified string.
var stringLength = function(anyString) {
  // Return the length of the string.
  return anyString.length;
};

// Export it.
export default stringLength;
