// Checks if value is greater than  or equal to other.
// Create function named _gte
function _gte (value1, value2) {
// Check to see if value1 is greater than or equal to value2
  if (value1 >= value2) {
    return true
  }
  else return false
}

// Export it.
export default _gte;
