// Checks if value is less than or equal to other.
// Create function named _lte
function _lte (value, other) {
  // Return whether value is less than or equal to other
  return value <= other;
}

// Export it.
export default _lte;
